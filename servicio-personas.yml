server:
  port: 8000
  servlet:
    context-path: /personas
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8000
server:
  servlet:
    context-path: /personas      
